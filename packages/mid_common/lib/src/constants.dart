/// The key for the request id that's added to requests headers
const requestIdKey = 'X-Request-ID';

/// The key for the request content type that's added to requests headers
const contentTypeKey = 'content-type';
