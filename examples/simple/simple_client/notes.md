

1- create client project
2- add the to `pubspec.yaml`: 
    - http
    - meta (for `@immutable` annotation)
    - collections (for data classes serialization)
3- create folder 'lib/src'
4- add folder for each api class (e.g. auth.dart, app.dart, stoage.dart)
5- create folder for data (e.g. session.dart, user.dart, etc)
